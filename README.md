# auto_tweet
auto_tweet bot
Make file keys.json in same directory which should contains key files as below.

{
    "consumer_key": "",
    "consumer_secret" : "",
    "access_token" : "",
    "access_token_secret" : ""
}
